runpod>=1.7
requests>=2.32
numpy>=2.1
opencv-python-headless>=4.10
# Pinned (TASK-034): the tracker lifecycle fix relies on verified 8.4.70
# callback semantics (register-once persist capture, BYTETracker.reset id
# behavior); '>=8.3' let every image rebuild pick up silent tracker drift.
ultralytics==8.4.70
pandas>=2.0
Pillow>=10.0
tqdm>=4.66
parse>=1.20
# TrackNetV3 predict.py import chain (utils.general) needs these — without them
# predict.py exits 1 at import on the GPU worker (matches the local vision deps).
pycocotools>=2.0
matplotlib>=3.7

# ultralytics ByteTrack (model.track) linear assignment (TASK-024)
lap>=0.5.12
